[phases.setup]
nixPkgs = ["python39", "python39Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn dashboard:app --bind 0.0.0.0:$PORT --timeout 120 --worker-class gevent --workers 1"
